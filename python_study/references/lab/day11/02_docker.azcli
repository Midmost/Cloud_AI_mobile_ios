#0. Dockerfile 확인
#1. 도커 이미지 빌드
# mac에서는 sudo빼고
sudo docker image build ./firstNodeDockerApp -t firstnode-app:1.0
sudo docker build ./GuestBook4ExpressJS -t guestbook-app

#2. 이미지 확인
sudo docker images

